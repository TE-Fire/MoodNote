<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moodnote.mapper.TagMapper">

    <!-- 根据日记ID查询标签列表 -->
    <select id="selectTagsByDiaryId" resultType="com.moodnote.pojo.vo.TagVO">
        SELECT 
            t.id, t.name, t.color, t.create_time
        FROM mood_diary_tag dt
        LEFT JOIN mood_tag t ON dt.tag_id = t.id
        WHERE dt.diary_id = #{diaryId}
          AND dt.deleted = 0
          AND t.deleted = 0
        ORDER BY t.create_time DESC
    </select>

</mapper>
